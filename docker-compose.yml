services:
  uretos-cockpit:
    build: .
    container_name: uretos-cockpit
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      VITE_GATEWAY_URL: "http://localhost:8080"
    stdin_open: true
    tty: true